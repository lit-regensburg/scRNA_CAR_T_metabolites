source_up

ANALYSIS_DIR=$(dirname $(find_up .analysisroot))

if [ -z "${ANALYSIS_DIR}" ]
then
    ANALYSIS_DIR=$PROJECT_DIR
    echo No .analysisroot file found, setting project directoy as analysis directory
fi

export ANALYSIS_DIR

SINGULARITYENV_ANALYSIS_DIR=$ANALYSIS_DIR
export SINGULARITYENV_ANALYSIS_DIR
